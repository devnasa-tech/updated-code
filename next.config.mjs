/** @type {import('next').NextConfig} */
const nextConfig = {
  
  images: {
    domains: [
      "i.ibb.co",
      "images.unsplash.com",
      "plus.unsplash.com",
      "localhost",
      "y-lac-seven.vercel.app", 
    ],
  },
};

export default nextConfig;
